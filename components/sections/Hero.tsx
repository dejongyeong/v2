import Intro from "../shared/hero/Intro";
import Navigation from "../shared/hero/Navigation";

const Hero = () => {
  return (
    <div>
      <Navigation />
      <Intro />
    </div>
  );
};

export default Hero;
